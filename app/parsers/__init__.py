"""Пакет парсеров источников цен.

Архитектура заложена как рабочие заготовки:
- BaseParser — общий интерфейс;
- HttpHtmlParser — requests/aiohttp + BeautifulSoup (веб-парсинг, fallback);
- RssXmlParser — разбор RSS/XML/Atom-фидов магазинов;
- aggregator — собирает предложения из всех парсеров и пишет в БД.

ВАЖНО: конкретные URL/селекторы зависят от источника и могут меняться,
поэтому по умолчанию парсеры ОТКЛЮЧЕНЫ (ENABLED_PARSERS пуст), а бот
работает на демо-каталоге. Чтобы включить реальный источник — добавьте
его конфиг и включите в aggregator.ENABLED_PARSERS.
"""
